# Design Direction

This file records the chosen visual direction for the website — the output
of the `art-direction` skill (`.claude/skills/art-direction/SKILL.md`). Keep
it in sync with what's actually built: when the direction changes, update
this file in the same pass, not after.

This is a record of *decisions and rationale*, not a moodboard dump. Every
section should be answerable in plain language, grounded in confirmed facts
from `SITE-CONTENT-NOTES.md` — never invented history, imagery, or symbolism.

Status: **not yet established.** The three-directions proposal (art-direction
skill §7) has not been presented or chosen. Fill in this template once it
has.

---

## 1. Chosen direction

- **Name:**
- **Core concept:** (one or two sentences — what is this direction *about*)
- **Emotional feeling:** (what should a visitor feel in the first few
  seconds)
- **Why it fits this business:** (tie directly to real facts — the place,
  the family, the wine, the history — not generic "premium" reasoning)
- **Directions considered but not chosen:** (name the other two and the
  one-line reason each lost)

## 2. Visual motifs

List the 5–10 motifs from the motif library (art-direction skill §8) that
made it into the final direction. For each:

| Motif | What it is | Where it comes from | How it appears on the site | Why it belongs to this brand |
|---|---|---|---|---|
| | | | | |

## 3. Typography

- **Display/heading face:**
- **Body face:**
- **Pairing rationale:** (why these two, together)
- **Scale/hierarchy approach:** (oversized headings? cropped type? vertical
  type? standard scale? — reference art-direction skill §5)
- **Where typography becomes a visual element** (not just a container for
  words), if anywhere:

## 4. Color

- **Palette:** (list actual hex values, tied to the real brand — logo,
  labels, physical materials — not arbitrary choices)
- **Primary / secondary / accent roles:**
- **Where each color is used and why:**

## 5. Imagery

- **Photography direction:** (documentary? staged? archival? — and why)
- **Status of real photography:** (confirmed shots in hand / pending from
  owner — never substitute stock or AI-generated imagery; see `CLAUDE.md`)
- **Placeholder convention until real photos arrive:**
- **Treatment:** (crops, masks, duotone, grain, natural color — whatever
  applies)

## 6. Background & texture system

- **What backgrounds are made of on this site** (full-bleed photography,
  texture, pattern derived from a real motif, flat color, combination):
- **Explicit rule for what is *not* used** (e.g. no generic blobs/squiggles
  per art-direction skill §1) and what replaced it:

## 7. Layout archetypes

List the section composition types actually in use (art-direction skill
§6) and where each appears:

| Archetype | Used on | Why this composition here |
|---|---|---|
| | | |

## 8. Motion

- **Motion philosophy:** (restrained/editorial? nothing at all? something
  else — and why)
- **What animates, and what deliberately doesn't:**
- **Reduced-motion behavior:** (must degrade to fully visible content, per
  `CLAUDE.md` — content must never depend on JS/animation to be seen)

## 9. Responsive behavior

Not a shrink of desktop — a deliberate composition per breakpoint (see
art-direction skill §9).

| Breakpoint | What changes (crops, type scale, stacking, decoration, negative space) |
|---|---|
| 375px | |
| 768px | |
| 1024px | |
| 1440px | |

## 10. Design rationale / decision log

Running log of major visual decisions and the reasoning behind them —
append, don't rewrite history. Useful when a later session needs to
understand *why* something looks the way it does before changing it.

| Date | Decision | Reasoning |
|---|---|---|
| | | |
